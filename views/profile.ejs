<h2>Your Favorites</h2>
<% if (favorites && favorites.length > 0) { %>
  <ul>
    <% favorites.forEach(favorite => { %>
      <li>
        <h3><%= favorite.recipe_name %></h3>
        <img src="<%= favorite.recipe_thumbnail %>" alt="<%= favorite.recipe_name %>" />
      </li>
    <% }); %>
  </ul>
<% } else { %>
  <p>You have no favorites yet.</p>
<% } %>
